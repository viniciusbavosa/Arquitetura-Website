import { SlideOutAnimationDirective } from './slide-out-animation.directive';

describe('SlideOutAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideOutAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
